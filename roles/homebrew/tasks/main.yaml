---
- name: Install Homebrew autoupdate tool
  homebrew:
    tap: domt4/autoupdate
    # state: present

- name: Configure Homebrew autoupdate
  shell: |
    brew autoupdate start 43200 --upgrade --cleanup --immediate --sudo
  args:
    creates: ~/Library/LaunchAgents/homebrew.autoupdate.plist
